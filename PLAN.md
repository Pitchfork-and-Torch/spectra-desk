# Spectra Desk v9.0.0 Corroborate

Phase 0 found a real v8.2.0 desk: React client, Hono server, Electron Win64 shell, 898 toolkit rows, Fast cap 12.

v9 keeps that tree and adds:

- No auto-LOCK. LOCKED is a human action.
- Corroboration matrix and a SHA-256 claim ledger.
- Counsel and press redaction. DEMO case is the fictional composite Avery Quill.
- CLI and MCP beats: intake, divide, brief, lock, export, doctor, toolkit, case. Agents cannot LOCK.
- Hashed unsigned Win64 installer and portable zip.

The public site advertises a file only after the GitHub release asset returns HTTP 200.
